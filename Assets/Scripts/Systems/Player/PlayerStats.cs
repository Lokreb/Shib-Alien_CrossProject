using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerStats : MonoBehaviour
{
    /*private void Update()
    {
        if (Input.GetKeyDown(KeyCode.E) && bonusWeapon != null)
        {
            Debug.Log("Arme activée"); // Activate the bonus weapon when the E key is pressed
        }
    }*/

   /* public void AddBonusWeapon(Weapon bonus)
    {

        bonusWeapon.transform.parent = transform; // Attach the bonus weapon to the player
    }*/

    /*public void RemoveBonusWeapon()
    {
        if (bonusWeapon != null)
        {
            Destroy(bonusWeapon.gameObject); // Destroy the existing bonus weapon if there is one
            bonusWeapon = null;
        }
    }*/
}
